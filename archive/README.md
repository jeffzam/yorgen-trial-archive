# Version snapshots

Before each substantive hourly update, the updater stores the previously published `index.html` under `YYYY/MM/DD/HH00/`. Checks that find no new reporting do not create snapshots.

